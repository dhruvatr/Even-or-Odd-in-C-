#include <iostream>
using namespace std;

int main() {
    int n, digit, count = 0;

    cin >> n;

    while (n > 0) {
        digit = n % 10;

        if (digit % 2 == 0)
            count++;

        n /= 10;
    }

    cout << count;

    return 0;
}