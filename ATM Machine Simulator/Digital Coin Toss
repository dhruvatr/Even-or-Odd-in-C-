#include <iostream>
#include <cstdlib>
#include <ctime>

using namespace std;

int main() 
{
    srand(time(0));

    int toss = rand() % 2;

    cout << "Flipping the coin...\n\n";

    if(toss == 0)
        cout << "HEADS" << endl;
    else
        cout << "TAILS" << endl;

    return 0;
}