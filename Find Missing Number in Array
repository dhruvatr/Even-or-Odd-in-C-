#include <iostream>
using namespace std;

int main() {
    int arr[] = {1, 2, 3, 5};
    int n = 5;

    int total = n * (n + 1) / 2;
    int sum = 0;

    for(int i = 0; i < 4; i++) {
        sum += arr[i];
    }

    cout << "Missing number is: " << total - sum;

    return 0;
}