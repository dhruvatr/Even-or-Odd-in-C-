#include <iostream>
using namespace std;

int main() {
    int secret = 7;
    int guess;

    cout << "Guess a number btw 1 to 10:";
    cin >> guess;

    if(guess == secret) {
        cout << "It is correct:";
    }else{
        cout << "It is Incorrect" << secret;
    }
    return 0;
    }
